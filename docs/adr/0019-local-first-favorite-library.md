# Local-First Favorite Library

The Favorite Library will become a local-first collection of saved Yamibo content, with Yamibo remote favorites treated as a sync target rather than the source of truth. Adopting this model deliberately rebuilds the existing Favorite Library organization instead of preserving archived remote-first metadata, because preserving the old archive semantics would keep remote favorites as the hidden authority and conflict with multi-location local organization.

# Native forum has no user-selectable web mode

The forum tab will open the **Native Forum Surface** at **Forum Home** without a user-facing setting or menu item to switch back to the old WebView browser. `ForumBrowserView` remains available only as the internal **Forum Web Fallback** for posting, reader-origin forum links, unsupported URLs, login or verification flows, and parser failure recovery.
